# アプリケーション実行環境（local, development, staging, production）
# 現在は参照のみ。将来的にログレベルやデバッグ機能の制御に使用予定
APP_ENV=local

# アプリケーションのポート番号
APP_PORT=8080

# データベース設定（オプション1: ローカル開発用の個別変数）
DB_HOST=local
DB_PORT=5432
DB_USER=memoria
DB_PASSWORD=memoriapass
DB_NAME=memoria_db
DB_SSLMODE=disable

# データベース設定（オプション2: Railway/本番環境用のDATABASE_URL）
# DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Firebase 認証設定
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_API_KEY=

# 管理者メールアドレス（カンマ区切りで複数指定可能）
ADMIN_EMAILS=admin1@example.com,admin2@example.com

# フロントエンドのベースURL（招待リンク作成に使用）
FRONTEND_BASE_URL=https://localhost:3000

# SES 送信元メールアドレス（検証済みドメイン/アドレス）
SES_FROM_EMAIL=no-reply@your-domain.com

# 招待メール本文テンプレート（txtファイル）
# 置換可能: {{INVITE_URL}}, {{ROLE}}, {{EMAIL}}, {{GROUP_NAME}}, {{INVITE_TYPE}}
SES_INVITE_TEMPLATE_PATH=

# AWS S3 設定（画像ストレージ）
AWS_REGION=ap-northeast-1
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
